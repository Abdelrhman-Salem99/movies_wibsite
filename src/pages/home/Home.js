import { Box } from "@mui/material";
import React from "react";

const Home = () => {
  return (
    <Box
      sx={
        {
          // bgcolor: (theme) => theme.palette.primary.main,
        }
      }
    >
      Home
    </Box>
  );
};

export default Home;
