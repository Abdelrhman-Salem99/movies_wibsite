import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      light: "#131722",
      main: "#131722",
      dark: "#949cb0",
    },
    secondary: {
      main: "#F78A24",
    },
  },
});

export default theme;
